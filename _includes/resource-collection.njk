<div class="column is-12">
  <details>
    <summary class="title is-size-4 is-size-3-desktop">{{ resource.name }} - {{ resource.collection | length }}</summary>
    <ul>
    {% for item in resource.collection %}

        <li>
            <span class="subtitle is-5">{{ item.name }}</span>
            {% if item.url %}<br/><a href="{{ item.url }}" target="_blank">Website</a>{% endif %}
            {% if item.chat %} - <a href="{{ item.chat }}" target="_blank">Chat Online</a>{% endif %}
            {% if item.tel %}
                <br />
                <span>Phone: {{ item.tel }}</span>
            {% endif %}
            {% if item.sms %}
                <br />
                <span>Text: {{ item.sms }}</span>
            {% endif %}
            {% if item.instructions %}
                <br />
                <span>{{ item.instructions | replace('{sms}', item.sms)  }}</span>
            {% endif %}
            {% if item.language %}
                <br />
                <span>{{ item.language }}</span>
            {% endif %}
            <hr />
        </li>
    {% endfor %}
    </ul>
  </details>
</div>